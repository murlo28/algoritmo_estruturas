Inicio
    inteiro: soma, contador, valorInformado, limiteExecução;

    soma ←0;
    leia("Informe a quantidade de execução", limiteExecução);

    para contador de 1 ate 500 passo 1 faça
        leia(valorInformado);
            se(valorInformado mod 2 = 1)
                entao
                    Inicio
                        se(valorInformado mod 3 = 0)
                            entao soma ← soma + valorInformado;
                        fimse;
                    fim;
            fimse;
            
    fimpara;


Fim